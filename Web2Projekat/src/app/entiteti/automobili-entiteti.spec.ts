import { AutomobiliEntiteti } from './automobili-entiteti';

describe('AutomobiliEntiteti', () => {
  it('should create an instance', () => {
    expect(new AutomobiliEntiteti()).toBeTruthy();
  });
});
